#!/usr/bin/sh

while true; do
	ssid=$(essid -w wlo1)
	if [[ ! $ssid ]]; then
		ssid=$(netcfg current)
	fi
	echo -e "N${ssid}:$(cat /proc/net/wireless | sed '1,2d;s|.*\s\([0-9]*\)\..*|\1|')"
	sleep 1;
done
