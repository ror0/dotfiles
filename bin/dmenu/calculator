#!/usr/bin/sh

#xsel -o | dmenu -b -fn 'Inconsolata-9.5' -nb '#ffffff' -nf '#151515' -sf '#151515' -sb '#4E9FB1' -p Calculate: | xargs echo | bc 2>&1 | dmenu -b -fn 'Inconsolata-9.5' -nb '#ffffff' -nf '#151515' -sf '#151515' -sb '#4E9FB1' -p Answer: | xsel -i

. config_dmenu

ans="Press escape to exit."

while [[ $ans != "" ]]; do
	ans="$(echo $ans | dmenu -b -fn 'Inconsolata-9.5' -nb $ibg -nf $ifg -sf $sfg -sb $sbg -p 'Calculate:' | bc -l 2>&1)"
done
