#!/usr/bin/bash
#input="$(xsel -o | $HOME/.bin/dmenu -p "Bestand zoeken:" )"
#if [ "$input" != '' ]; then
#	result="$(echo "$input" | locate -e -r "$input" | $HOME/.bin/dmenu -p "Zoekresultaat:" )"
#	xdg-open "$result"
#fi

. config_dmenu

find ~/ | sed 's/ /\\ /g' | sort -f | dmenu -b -p "Search:" -i -l 10 -fn 'Inconsolata-9.5' -nb $ibg -nf $ifg -sf $sfg -sb $sbg | sed 's|/[^/]*$|/|' | xargs dir_xterm
